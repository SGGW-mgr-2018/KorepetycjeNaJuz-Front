const state = {
  searchQuery: ''
}

const getters = {}

const mutations = {
  SET_SEARCH_QUERY (state, payload) {
    state.searchQuery = payload
  }
}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
